% If x is a vector, this gives in y (vector) the largest n elements of x,% and in i (vector) their indeces in the vector. % If x is a matrix, it returns in y (n-row matrix) the largest n elements% of each column, and in i (n-row matrix) the positions of these maximums.   %% Usage: [y,i]=maxn(x,n)%% SEE ALSO: max (which is a special case of maxn when n=1) %				G. Tong Zhou%				Dec. 8, 1991function [y,i]=maxn(x,n)[sm,sn]=size(x);y=[];i=[];if sm~=1 & sn~=1 & n>sm   error('Not that many rows!')end if sm==1 | sn==1 & n>max(sm,sn)   error(['There are not ',num2str(n), ' local maxima in the vector!'])end     for j=1:n    [y1,i1]=max(x);    y=[y;y1];    i=[i;i1];     if sm==1 | sn==1        x(i1)=-inf;    else        for t=1:sn         x(i1(t),t)=-inf;       end     end end 