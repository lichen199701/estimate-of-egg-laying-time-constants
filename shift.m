% Shift the row indices of a given vector/matrix by an amount% k, positive (shift up/left) or negative (shift down/right). % If the matrix is f(a,b) before the shift, then F=shift(f,k) % has F(a,b)=f(a+k,b). If the matrix has N rows, then when k>0, % F(a,b)=0 for a>=N-k+1; and when k<0, F(a,b)=0 for a<=|k|. % % INPUT: f: original vector/matrix%        k: the amount of shift in the rows of f% Usage: F=shift(f,k)%					G. Tong Zhou%					May 19, 1994function F=shift(f,k)[m,n]=size(f);				% size of fif k==0   F=f;					% no shift   returnend if abs(k)>=m   F=zeros(m,n);			% too much shift    returnendif k>0  F=[f; zeros(m,n)];			% append at the bottom  F=F(1+k:m+k,:);			% retain the desired portion  returnend  if k<0   F=[zeros(m,n); f];			% append at the top  F=F(m+k+1:2*m+k,:);			% retain the desired portion  returnend 